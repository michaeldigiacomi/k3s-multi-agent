# Secrets for Appy (Application Architect) overlay
# Copy to secrets.env and fill in values, then:
#   kubectl create secret generic hermes-secrets --from-env-file=secrets.env -n hermes-appy
API_SERVER_KEY=change-me
OLLAMA_API_KEY=
DISCORD_BOT_TOKEN=<appy-bot-token>
