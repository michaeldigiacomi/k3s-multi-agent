# Example secret for the Ollama-local overlay.
# No external API key needed — Ollama runs in-cluster.
# Copy this to secrets.env and fill in your values, then:
#   kubectl create secret generic hermes-secrets --from-env-file=secrets.env -n hermes-ollama-local
#
API_SERVER_KEY=change-me